# GSRender RAG系统依赖包
# 核心依赖
faiss-cpu>=1.7.3      # CPU版本FAISS，如需GPU加速请安装faiss-gpu
scikit-learn>=1.0.0
scipy>=1.7.0
tqdm>=4.62.0

# 可视化和分析
matplotlib>=3.5.0
seaborn>=0.11.0

# 数据处理
pandas>=1.3.0  # 可选，用于数据分析

# 性能分析工具
memory-profiler>=0.60.0  # 可选，内存分析
line-profiler>=3.5.0     # 可选，性能分析

# 如果需要GPU加速FAISS (二选一)
# faiss-gpu>=1.7.3

# 如果需要更高级的聚类算法
# hdbscan>=0.8.28
# umap-learn>=0.5.3